[pytest]
testpaths = tests
python_files = test_*.py
addopts = --basetemp=.pytest-tmp -p no:cacheprovider
# Explicitly keep collection to Python test modules only; additional
# non-.py files starting with 'test_' will be ignored via conftest.py
